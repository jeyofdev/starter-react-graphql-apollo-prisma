type Book {
	id: ID
	title: String
	author: String
	year: Int
}

type Query {
	books: [Book]
}

type AddBookMutationResponse {
	code: String!
	success: Boolean!
	message: String!
	book: Book
}

type Mutation {
	addBook(title: String, author: String, year: Int): AddBookMutationResponse
}
